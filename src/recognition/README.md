# Face Recognition Module

## Purpose
Face embedding comparison, identity matching and known/unknown classification.

## Scope for MVP
- Calculate vector distance / similarity scores between detected face embeddings and registered gallery embeddings.
- Classify identity based on similarity threshold (e.g. status: VERIFIED vs UNKNOWN).
- Output identity metadata (Name, Student ID, Match Score, Status).
